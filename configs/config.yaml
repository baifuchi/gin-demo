# 可通过环境变量 CONFIG_PATH 指定路径，默认 configs/config.yaml

server:
  addr: ":8080"
  # gin 模式: debug | release | test
  mode: "debug"

rabbitmq:
  host: "47.110.35.169"
  vhost: "/"
  port: 5672
  login: "admin"
  password: "admin"
  # 与 PHP 一致，单位：秒
  heartbeat: 800

app:
  queue_name: "gin_demo_queue"
  data_dir: "data"
  consumer_count: 3
